import { LogHighLightPipe } from './log-high-light.pipe';

describe('LogHighLightPipe', () => {
  it('create an instance', () => {
    const pipe = new LogHighLightPipe();
    expect(pipe).toBeTruthy();
  });
});
